package com.example.assignment3_recipeapp.page.navbar
/**
 * Enumerates the routes used in the administration section of the application.
 *
 * Each enum constant represents a specific administrative screen or functionality
 * within the application, facilitating navigation and maintenance of routing paths.
 */
enum class AdminRoutes(val value: String) {
    ManageUser("ManageUser"),
    Report("Report"),
}